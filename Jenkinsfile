#!/usr/local/bin/groovy
pipeline {
    agent { node { label 'master' } }

    environment {
        TEST_PREFIX = "test-IMAGE"
        
    }

    stages {
        
          stage("Build and start test image") {
            steps {
                sh "ls"
                
                
            }
        }

        stage("Run tests") {
            steps {
                sh "ls"
                
            }
        }

    }
}